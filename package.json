{
  "name": "web-tools",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://nickstanish.com/web-tools",
  "dependencies": {
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm-run-all -s build:js build:docs",
    "build:js": "react-scripts build",
    "build:docs": "rm -rf docs && cp -r build docs",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
